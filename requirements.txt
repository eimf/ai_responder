# ============================================================
# AI Responder — Python dependencies
# Install with:  pip install -r requirements.txt
# ============================================================

# ---- UI Framework ----
PyQt6>=6.6.0

# ---- Phase 2: Screen Reading ----
uiautomation>=2.0.18       # Windows UIAutomation tree traversal
pywin32>=306               # Win32 API (GetForegroundWindow, etc.)
mss>=9.0.1                 # Fast screen capture
pytesseract>=0.3.10        # Tesseract OCR wrapper (requires Tesseract binary)
Pillow>=10.0.0             # Image processing for OCR

# ---- Phase 3: AI Integration ----
openai>=1.30.0                 # OpenAI + Azure OpenAI SDK
